import { ComingSoon } from '../components/ComingSoon.js';

export function ListsPage(): JSX.Element {
  return (
    <ComingSoon
      title="Custom lists"
      icon="list-ul"
      description="Shopping, packing, anything else the household keeps on paper on the fridge door."
    />
  );
}
