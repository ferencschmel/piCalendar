import { ComingSoon } from '../components/ComingSoon.js';

export function TasksPage(): JSX.Element {
  return (
    <ComingSoon
      title="Tasks"
      icon="check2-square"
      description="Chores and to-dos for the household, alongside the calendar that says when they are due."
    />
  );
}
